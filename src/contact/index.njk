---
layout: layouts/marketing-page.njk
title: "Contact"
description: "Get in touch with VoiceTel — sales, support, partnerships, billing, and press."
---

<p class="eyebrow">Contact</p>
<h1>Contact VoiceTel</h1>
<p class="lede">Sales, support, partnerships, billing, and press inquiries. Pick the right channel and we'll route you to the right person.</p>

<p>For account-specific issues, sign in to the <a href="{{ site.portalUrl }}">portal</a> and use the in-app support form so we can attach your account context automatically.</p>

<p>For everything else, email <a href="mailto:{{ site.supportEmail }}">{{ site.supportEmail }}</a>.</p>

<p>Phase 1 placeholder. The full contact page lands per the brief's <code>§7</code> spec — separate forms for sales, support, partnerships, billing, and press, each posting to <code>{{ site.portalUrl }}/contact</code> with the right routing tag.</p>
