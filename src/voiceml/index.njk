---
layout: layouts/page.njk
title: "VoiceML"
description: "VoiceML — VoiceTel's drop-in compatible voice markup runtime. Move your Twilio voice apps to VoiceTel without rewriting code."
---

<p class="eyebrow">Voice markup runtime</p>
<h1>VoiceML</h1>
<p class="lede">VoiceTel's voice markup runtime. Compatible with Twilio's voice markup so existing apps move over without rewriting code. <code>&lt;Say&gt;</code>, <code>&lt;Gather&gt;</code>, <code>&lt;Dial&gt;</code>, <code>&lt;Record&gt;</code>, and the rest work unchanged.</p>

<p>Phase 1 placeholder. Full content lands per the brief's <code>§7 /voiceml/</code> spec.</p>

<p class="hero-actions">
	<a class="btn btn-primary" href="/voiceml/migrate/">Migrate from Twilio</a>
	<a class="btn btn-secondary" href="/voiceml/compatibility/">Compatibility matrix</a>
</p>
