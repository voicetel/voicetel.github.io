---
layout: layouts/page.njk
title: "Open-source PBX platforms"
description: "SIP trunk configuration for FreePBX, FusionPBX, 3CX, and raw Asterisk PBX deployments connecting to VoiceTel."
---

<p class="eyebrow">Support / PBX</p>
<h1>Open-source PBX trunk configuration.</h1>
<p class="lede">Most SIP-PBX trunk wizards collect the same fields from the operator and write them into the underlying engine. The values are the same across vendors; the wizard layout is what changes.</p>
<p class="hero-actions">
	<a class="btn btn-primary" href="/contact/">Contact engineering</a>
	<a class="btn btn-secondary" href="/sip-trunking/">SIP trunking overview</a>
</p>

<section class="section-block">
	<h2>Common trunk fields</h2>
	<ul>
		<li><strong>Trunk name:</strong> a label like <code>VoiceTel</code></li>
		<li><strong>SIP server / Registrar:</strong> the SIP server host shown in your customer portal from your onboarding email</li>
		<li><strong>Outbound proxy:</strong> same as SIP server</li>
		<li><strong>Username / Auth user:</strong> SIP username from VoiceTel onboarding</li>
		<li><strong>Password / Secret:</strong> SIP password from onboarding</li>
		<li><strong>From domain:</strong> the SIP server host shown in your customer portal</li>
		<li><strong>Transport:</strong> UDP</li>
		<li><strong>Authentication:</strong> Digest (most common) or IP authentication</li>
		<li><strong>Register interval:</strong> 60 seconds for tight-NAT networks; 600+ otherwise</li>
		<li><strong>Qualify / OPTIONS ping:</strong> 30 seconds</li>
		<li><strong>Codecs:</strong> ulaw (G.711µ), alaw (G.711a), opus where supported</li>
		<li><strong>DTMF:</strong> RFC 4733 / RFC 2833 (in-RTP)</li>
	</ul>
</section>

<section class="section-block">
	<h2>Platforms</h2>
	<ul class="features-grid">
		<li class="feature">
			<h3><a href="/support/pbx/freepbx/">FreePBX</a></h3>
			<p>Sangoma FreePBX / PBXact. Most common SOHO open-source PBX.</p>
		</li>
		<li class="feature">
			<h3><a href="/support/pbx/fusionpbx/">FusionPBX</a></h3>
			<p>FreeSWITCH-based multi-tenant PBX with web admin.</p>
		</li>
		<li class="feature">
			<h3><a href="/support/pbx/3cx/">3CX</a></h3>
			<p>Commercial SIP PBX with a SIP trunk wizard for many providers.</p>
		</li>
		<li class="feature">
			<h3><a href="/support/sbc/asterisk/">Asterisk (raw)</a></h3>
			<p>For raw <code>pjsip.conf</code> configuration without a GUI, see the Asterisk SBC guide.</p>
		</li>
	</ul>
</section>
