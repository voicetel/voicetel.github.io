---
layout: layouts/base.njk
title: ""
description: "Run your Twilio app on VoiceTel. Same voice markup. Same webhook signatures. Up to 30% less on list rates, with volume discounts that scale with usage."
---
<section class="hero container">
	<p class="eyebrow">Programmable voice platform</p>
	<h1>Run your Twilio app on VoiceTel.</h1>
	<p class="lede">Same voice markup. Same webhook signatures. Up to 30% less on list rates, with volume discounts that scale with usage. Point your existing voice URL at our endpoint and your <code>&lt;Say&gt;</code>, <code>&lt;Gather&gt;</code>, <code>&lt;Dial&gt;</code>, and <code>&lt;Record&gt;</code> work unchanged.</p>
	<p class="hero-actions">
		<a class="btn btn-primary" href="/voiceml/migrate/">Migrate in 5 minutes</a>
		<a class="btn btn-secondary" href="/voiceml/compatibility/">Compatibility matrix</a>
	</p>
</section>

{% include "partials/menu-carousel.njk" %}

<section class="container">
	<p>This is the Phase 1 scaffold landing page. Hero panels, comparison block, pricing snippet, use-case grid, trust strip, and signup CTA land in subsequent edits per the brief's <code>§7</code> homepage spec.</p>
</section>
