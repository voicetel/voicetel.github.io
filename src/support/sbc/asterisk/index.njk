---
layout: layouts/page.njk
title: "Asterisk SIP trunk to VoiceTel"
description: "Configure Asterisk PJSIP endpoint, AOR, and identify objects to trunk to VoiceTel — Digest or IP authentication."
---

<p class="eyebrow">Support / SBC / Asterisk</p>
<h1>Asterisk SIP trunk to VoiceTel.</h1>
<p class="lede">Asterisk's PJSIP stack uses object-oriented config: Endpoint, AOR, Auth, Identify, and Registration. The pjsip.conf snippet below handles a VoiceTel trunk with Digest or IP authentication.</p>

<section class="section-block">
	<h2>pjsip.conf — Digest authentication</h2>
	<pre class="code-block" data-lang="ini"><code>[voicetel-auth]
type = auth
auth_type = userpass
username = &lt;sip-username&gt;
password = &lt;sip-password&gt;

[voicetel-aor]
type = aor
contact = sip:&lt;sip-host-from-portal&gt;
qualify_frequency = 30

[voicetel-identify]
type = identify
endpoint = voicetel
match = &lt;sip-host-from-portal&gt;

[voicetel-registration]
type = registration
outbound_auth = voicetel-auth
server_uri = sip:&lt;sip-host-from-portal&gt;
client_uri = sip:&lt;sip-username&gt;@&lt;sip-host-from-portal&gt;
expiration = 600
retry_interval = 60

[voicetel]
type = endpoint
context = from-voicetel
disallow = all
allow = ulaw,alaw,g729
outbound_auth = voicetel-auth
aors = voicetel-aor
from_domain = &lt;sip-host-from-portal&gt;
direct_media = no
trust_id_inbound = yes
send_pai = yes
rtp_symmetric = yes
force_rport = yes
rewrite_contact = yes
dtmf_mode = rfc4733</code></pre>
</section>

<section class="section-block">
	<h2>pjsip.conf — IP authentication</h2>
	<p>For IP-auth trunks, omit the Auth and Registration objects and add the VoiceTel source IP to the Identify match list:</p>
	<pre class="code-block" data-lang="ini"><code>[voicetel-aor]
type = aor
contact = sip:&lt;sip-host-from-portal&gt;
qualify_frequency = 30

[voicetel-identify]
type = identify
endpoint = voicetel
match = &lt;sip-source-ip-from-portal&gt;
match = &lt;sip-host-from-portal&gt;

[voicetel]
type = endpoint
context = from-voicetel
disallow = all
allow = ulaw,alaw,g729
aors = voicetel-aor
from_domain = &lt;sip-host-from-portal&gt;
direct_media = no
trust_id_inbound = yes
send_pai = yes
rtp_symmetric = yes
force_rport = yes
rewrite_contact = yes
dtmf_mode = rfc4733</code></pre>
</section>

<section class="section-block">
	<h2>extensions.conf — outbound bridge</h2>
	<pre class="code-block" data-lang="ini"><code>[from-internal]
exten =&gt; _NXXNXXXXXX,1,NoOp(Outbound to VoiceTel)
 same =&gt; n,Set(CALLERID(num)=${YOUR_DID})
 same =&gt; n,Dial(PJSIP/${EXTEN}@voicetel,60)
 same =&gt; n,Hangup()</code></pre>
</section>

<section class="section-block">
	<h2>extensions.conf — inbound landing context</h2>
	<pre class="code-block" data-lang="ini"><code>[from-voicetel]
exten =&gt; _.,1,NoOp(Inbound from VoiceTel)
 same =&gt; n,Goto(internal,${EXTEN},1)
 same =&gt; n,Hangup()</code></pre>
</section>

<section class="section-block">
	<h2>Verify</h2>
	<pre class="code-block" data-lang="bash"><code># Asterisk CLI
*CLI&gt; pjsip reload
*CLI&gt; pjsip show registrations
*CLI&gt; pjsip show endpoints
*CLI&gt; pjsip set logger on    # SIP trace</code></pre>
</section>

<aside class="cta-block" aria-labelledby="asterisk-cta-heading">
	<h2 id="asterisk-cta-heading">FreePBX or FusionPBX user?</h2>
	<p>If you're using a GUI on top of Asterisk, see the <a href="/support/pbx/">PBX guides</a> for FreePBX and FusionPBX-specific trunk configurations.</p>
	<p class="hero-actions">
		<a class="btn btn-primary" href="/contact/">Contact engineering</a>
	</p>
</aside>
