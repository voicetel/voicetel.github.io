---
layout: layouts/page.njk
permalink: /docs/api/v7.8/csp/playground/
title: "10DLC Campaign Registry playground"
description: "Browser-based playground for the 10DLC Campaign Registry (TCR / CSP) API at api.voicetel.com/v2/csp. Set your VoiceTel API key once — encrypted to this device — and run live GET requests against the proxy without leaving the page."
---

<p class="eyebrow"><a href="/docs/api/">API reference</a> / <a href="{{ apiSpecs.csp.urlPath }}">10DLC Campaign Registry</a> / Playground</p>
<h1>10DLC Campaign Registry playground.</h1>
<p class="lede">Set your VoiceTel API key once. It's encrypted to this device and this browser, never sent to a VoiceTel server, and never readable from this page. The Campaign Service Provider proxy is at <code>api.voicetel.com/v2/csp</code>.</p>

<div class="alert alert-info" role="note">
	<p>Your API key is encrypted on this device and stored in this browser. Once saved, it cannot be viewed here &mdash; only used to authorize requests you send, replaced, or removed. The same key authorizes every VoiceTel API, including v2.2; setting it here is the same key store used by the <a href="/docs/api/v2.2/playground/">v2.2 playground</a>.</p>
</div>

<section class="section-block playground" data-api-playground data-spec-url="/api-specs/csp.json">
	<header class="playground-header">
		<div class="playground-key">
			<h2>API key</h2>
			<p data-key-status></p>
			<div class="playground-key-actions" data-key-actions></div>
			<form class="playground-key-form" data-key-form hidden autocomplete="off">
				<h3 data-key-form-title>Set API key</h3>
				<p class="form-note">Pasted here, encrypted immediately, then this field is cleared. The plaintext key is never echoed back.</p>
				<div class="form-row form-row--full">
					<label for="playground-key-input">VoiceTel API key</label>
					<input id="playground-key-input" type="password" data-key-input autocomplete="off" spellcheck="false" required>
				</div>
				<p class="form-error" data-key-error></p>
				<div class="form-actions">
					<button type="submit" class="btn btn-primary">Save</button>
					<button type="button" class="btn btn-secondary" data-key-cancel>Cancel</button>
				</div>
				<p class="form-note">Need a key? See <a href="/docs/api/v2.2/credentials/">API credentials</a> for retrieval instructions.</p>
			</form>
		</div>
	</header>

	<div class="playground-layout">
		<aside class="playground-sidebar" aria-label="Operations">
			<h2>Operations</h2>
			<div class="playground-op-picker" data-op-picker></div>
		</aside>

		<div class="playground-main">
			<section class="playground-op" aria-labelledby="playground-op-title">
				<header class="playground-op-header">
					<h2 id="playground-op-title" data-op-title>Choose an operation</h2>
					<p class="playground-op-meta" data-op-meta></p>
					<p class="playground-op-summary" data-op-summary></p>
				</header>

				<form class="playground-op-form" data-op-form onsubmit="return false;"></form>

				<div class="playground-actions">
					<button type="button" class="btn btn-primary" data-op-send>Send</button>
				</div>

				<section class="playground-response" aria-label="Response">
					<h3>Response</h3>
					<div data-op-response></div>
				</section>

				<section class="playground-curl" aria-label="curl">
					<div class="playground-curl-header">
						<h3>curl</h3>
						<button type="button" class="btn btn-secondary btn-sm" data-op-copy>Copy</button>
					</div>
					<pre class="code-block" data-lang="bash"><code data-op-curl></code></pre>
					<p class="form-note">The copied command uses the literal string <code>YOUR_API_KEY</code>. Your stored key is never substituted into this preview.</p>
				</section>
			</section>
		</div>
	</div>
</section>

<aside class="cta-block" aria-labelledby="playground-cta-heading">
	<h2 id="playground-cta-heading">New to A2P 10DLC?</h2>
	<p>The Campaign Registry (TCR) is the industry consortium that vets brands and campaigns before they can run A2P messaging traffic. VoiceTel proxies the CSP API so you can register brands, file campaigns, and respond to vetting feedback through one credential.</p>
	<p class="hero-actions">
		<a class="btn btn-primary" href="/signup/">Free trial</a>
		<a class="btn btn-secondary" href="{{ apiSpecs.csp.urlPath }}">API reference</a>
	</p>
</aside>
