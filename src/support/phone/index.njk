---
layout: layouts/page.njk
title: "Hardware IP phones"
description: "SIP account configuration for Yealink, Poly/Polycom, Cisco MPP, Grandstream, and Snom IP desk phones working with VoiceTel SIP trunking."
---

<p class="eyebrow">Support / Phone</p>
<h1>Hardware IP phone configuration.</h1>
<p class="lede">Most SIP desk phones expose the same handful of account fields. The defaults differ vendor to vendor; the values to set them to don't.</p>
<p class="hero-actions">
	<a class="btn btn-primary" href="/contact/">Contact engineering</a>
	<a class="btn btn-secondary" href="/sip-trunking/">SIP trunking overview</a>
</p>

<section class="section-block">
	<h2>Common fields, recommended values</h2>
	<ul>
		<li><strong>Transport</strong> — UDP for most deployments. Use TLS (5061) where the firewall blocks UDP or registration drops are persistent.</li>
		<li><strong>SIP server / proxy</strong> — Provided in your VoiceTel onboarding (Anycast or Geocast assignment).</li>
		<li><strong>Outbound proxy</strong> — Same value as the SIP server unless engineering specified otherwise.</li>
		<li><strong>Username / Auth name</strong> — Per-trunk SIP credentials (not the portal account).</li>
		<li><strong>Register expiry</strong> — 60 seconds if your firewall has a short UDP NAT timeout; 3600 otherwise.</li>
		<li><strong>NAT keepalive</strong> — Enable; 30 seconds. Most vendors call this "keepalive interval" or "NAT traversal."</li>
		<li><strong>DTMF</strong> — RFC 2833 (in-RTP). Use SIP INFO only if your application explicitly requires it.</li>
		<li><strong>Codecs</strong> — G.711µ (PCMU) primary, G.711a (PCMA) fallback, OPUS where supported.</li>
		<li><strong>SRTP</strong> — Optional; use with TLS transport for encrypted media.</li>
	</ul>
</section>

<section class="section-block">
	<h2>Vendors</h2>
	<ul class="features-grid">
		<li class="feature">
			<h3><a href="/support/phone/yealink/">Yealink</a></h3>
			<p>T46U, T48S, T54W, T57W, T33G — most common SOHO/business desk phones.</p>
		</li>
		<li class="feature">
			<h3><a href="/support/phone/polycom/">Poly / Polycom</a></h3>
			<p>VVX 250, 350, 450, 601 — Edge series.</p>
		</li>
		<li class="feature">
			<h3><a href="/support/phone/cisco-mpp/">Cisco MPP</a></h3>
			<p>Cisco IP Phone 7800/8800 series running Multi-Platform (MPP) firmware.</p>
		</li>
		<li class="feature">
			<h3><a href="/support/phone/grandstream/">Grandstream</a></h3>
			<p>GRP series, HT80x ATAs, GXP series legacy phones.</p>
		</li>
	</ul>
</section>

<aside class="cta-block" aria-labelledby="phone-cta-heading">
	<h2 id="phone-cta-heading">Need provisioning at scale?</h2>
	<p>VoiceTel can provide auto-provisioning configs (HTTPS XML or SIP redirect) for hundreds-of-phone deployments. Contact engineering with your phone model and rollout count.</p>
	<p class="hero-actions">
		<a class="btn btn-primary" href="/contact/">Contact engineering</a>
	</p>
</aside>
