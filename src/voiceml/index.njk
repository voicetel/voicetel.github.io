---
layout: layouts/page.njk
title: "VoiceML"
description: "VoiceML is VoiceTel's XML-based Programmable Voice runtime for compatible call control, signed webhooks, recordings, conferences, queues, and SDK migration paths."
---

<p class="eyebrow">Programmable Voice, VoiceML</p>
<h1>Move compatible Programmable Voice workloads without rewriting call logic.</h1>
<p class="lede">VoiceML is VoiceTel's XML-based voice runtime for teams that already have voice markup, callbacks, recordings, conferences, queues, and API-driven call flows. Keep the interaction model; move the economics and support path to VoiceTel.</p>
<p class="lede">Build voice workflows on VoiceTel without paying retail markup on every layer of the call. Use API-driven calling, SIP access, recordings, conferences, streams, transcriptions, queues, and status callbacks on a platform built for telecom teams.</p>
<p class="hero-actions">
	<a class="btn btn-primary" href="/voiceml/migrate/">Start migration</a>
	<a class="btn btn-secondary" href="/voiceml/compatibility/">Compatibility matrix</a>
</p>

<section class="section-block code-block-section">
	<h2>VoiceML example</h2>
	<p>The markup stays readable, inspectable, and easy to test before cutover. A small inbound IVR — gather a digit, route to a queue, fall back to voicemail:</p>
	<pre class="code-block" data-lang="xml"><code>&lt;?xml version="1.0" encoding="UTF-8"?&gt;
&lt;Response&gt;
    &lt;Say voice="Polly.Joanna"&gt;Welcome to Acme Support. Press 1 for billing, 2 for technical support.&lt;/Say&gt;
    &lt;Gather numDigits="1" action="/route" method="POST" timeout="6"&gt;
        &lt;Say&gt;You can also stay on the line to reach the next available agent.&lt;/Say&gt;
    &lt;/Gather&gt;
    &lt;Dial timeout="20"&gt;
        &lt;Queue&gt;support&lt;/Queue&gt;
    &lt;/Dial&gt;
    &lt;Record maxLength="120" transcribe="false" action="/voicemail" /&gt;
    &lt;Say&gt;Thanks. We'll follow up within one business day.&lt;/Say&gt;
&lt;/Response&gt;</code></pre>
	<p>Webhook signatures, parameter names, and verb behavior match Twilio's voice markup, so an existing handler at <code>/route</code> or <code>/voicemail</code> works unchanged once you point your application's host at <code>voiceml.voicetel.com</code>.</p>
</section>

<section class="section-block">
	<h2>Compatible voice surface</h2>
	<ul class="features-grid">
		<li class="feature">
			<span class="feature-icon" aria-hidden="true">{% icon "telephone" %}</span>
			<h3>Calls</h3>
			<p>Create, fetch, update, list, and end calls using the supported voice-call lifecycle surface.</p>
		</li>
		<li class="feature">
			<span class="feature-icon" aria-hidden="true">{% icon "code-slash" %}</span>
			<h3>VoiceML call control</h3>
			<p>Answer, route, record, gather, dial, queue, conference, redirect, stream, and transcribe calls using XML-based voice markup.</p>
		</li>
		<li class="feature">
			<span class="feature-icon" aria-hidden="true">{% icon "activity" %}</span>
			<h3>Recordings</h3>
			<p>Start, pause, resume, stop, list, retrieve, and delete supported recordings.</p>
		</li>
		<li class="feature">
			<span class="feature-icon" aria-hidden="true">{% icon "diagram-3" %}</span>
			<h3>Conferences and queues</h3>
			<p>Use conference resources, participants, recordings, queue CRUD, and queue members where supported.</p>
		</li>
		<li class="feature">
			<span class="feature-icon" aria-hidden="true">{% icon "broadcast-pin" %}</span>
			<h3>Streams, transcriptions, and SIP recording</h3>
			<p>Start and inspect supported live media, transcription, and SIP recording resources with documented caveats.</p>
		</li>
		<li class="feature">
			<span class="feature-icon" aria-hidden="true">{% icon "gear" %}</span>
			<h3>Applications</h3>
			<p>Save voice-only application defaults and resolve them from call creation where supported.</p>
		</li>
		<li class="feature">
			<span class="feature-icon" aria-hidden="true">{% icon "arrow-right-circle" %}</span>
			<h3>Signed webhooks</h3>
			<p>Status, recording, gather, dial, conference, AMD, and action callbacks use signed webhook delivery for request verification.</p>
		</li>
		<li class="feature">
			<span class="feature-icon" aria-hidden="true">{% icon "plug" %}</span>
			<h3>Bring your own media vendors</h3>
			<p>Use your preferred speech-to-text, text-to-speech, and recording storage providers; VoiceTel does not hide third-party provider costs inside vague platform claims.</p>
		</li>
	</ul>
</section>

<section class="section-block" data-section-type="honest-differences">
	<h2>What's different</h2>
	<ul>
		<li><strong>Voice-focused compatibility</strong> — The compatible SDK surface is voice-focused. Messaging, number provisioning, pricing APIs, and other unrelated CPaaS products use native VoiceTel product APIs or portal workflows.</li>
		<li><strong>Unsupported payment verb</strong> — The payment verb is intentionally not implemented because PCI scope belongs in a fully certified payment flow.</li>
		<li><strong>Number provisioning path</strong> — DIDs are managed through VoiceTel number workflows, not through the compatible customer-facing voice API surface.</li>
	</ul>
</section>

<section class="section-block faq-block">
	<h2>VoiceML FAQ</h2>
	<details class="faq-item">
		<summary>Can existing Twilio voice applications migrate?</summary>
		<p>Yes, when they stay within the supported VoiceML-compatible voice surface. Review the compatibility matrix before production cutover.</p>
	</details>
	<details class="faq-item">
		<summary>Does VoiceTel provide speech services directly?</summary>
		<p>Bring your own speech provider. Supported integrations include:</p>
		<ul>
			<li>OpenAI TTS</li>
			<li>Amazon Polly</li>
			<li>Google Cloud TTS</li>
			<li>ElevenLabs</li>
			<li>Azure Cognitive Services</li>
		</ul>
		<p>VoiceTel also provides mod_flite (FreeSWITCH bundled) as the system default at no charge.</p>
	</details>
	<details class="faq-item">
		<summary>How are rates displayed?</summary>
		<p>Current public rates are on the <a href="/pricing/">pricing page</a>, with a calculator that applies volume-tier discounts. Your customer portal displays the same rates plus any account-specific contract pricing.</p>
	</details>
</section>

<section class="section-block" data-section-type="advantages">
	<h2>What's better</h2>
	<ul>
		<li><strong>Line-item pricing.</strong> Every rate published on the <a href="/pricing/">pricing page</a>. Speech, recording storage, and TTS billed by you directly to your chosen provider — VoiceTel does not mark up third parties.</li>
		<li><strong>Carrier operator since 1999.</strong> Twenty-seven years of running PSTN traffic. Engineers who read SIP traces, not qualification scripts.</li>
		<li><strong>No platform tax.</strong> Inbound messaging included. The published per-minute rate is what gets billed — no per-minute platform fee on top.</li>
		<li><strong>Honest compatibility.</strong> Every supported verb and attribute labelled in the <a href="/voiceml/compatibility/">compatibility matrix</a>. No silent feature gaps mid-migration.</li>
	</ul>
</section>

<aside class="cta-block">
	<h2>Start with one production call flow.</h2>
	<p>Pick a known call path, paste the markup into the compatibility checker, run a test call, then compare the bill line by line.</p>
	<p class="hero-actions">
		<a class="btn btn-primary" href="/voiceml/migrate/">Migrate from Twilio</a>
		<a class="btn btn-secondary" href="/voiceml/calculator/">Open calculator</a>
	</p>
</aside>
